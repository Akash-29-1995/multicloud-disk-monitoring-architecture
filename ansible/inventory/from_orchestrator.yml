# Optional static inventory pointing at latest generated file.
# Prefer: ansible-playbook -i inventory/generated/<file>.ini ...

# This file is a placeholder so operators know both paths exist:
#   1) inventory/aws_ec2.yml     — direct AWS dynamic inventory (MVP reviewer path)
#   2) inventory/generated/*.ini — orchestrator-normalized inventory (multi-customer path)
